# Override tool paths for run-tests.sh.
# Copy to .env.testing and uncomment/edit as needed.
# Environment variables take precedence over this file.
#
# If DDEV is detected, PHP/Composer default to the DDEV container:
#   PHP_BIN="ddev php"
#   COMPOSER_BIN="ddev composer"
# Node/npm always use local host binaries (node_modules has host-native deps).
#
# PHP_BIN=php
# COMPOSER_BIN=composer
# NODE_BIN=node
# NPM_BIN=npm
